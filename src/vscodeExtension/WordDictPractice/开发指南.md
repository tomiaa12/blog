# 开发、打包和发布指南

## 🛠️ 开发和运行

### 1. 在开发模式下运行

**方法一：使用 F5 快捷键（推荐）**

```bash
# 1. 确保在项目根目录打开VSCode（重要！）
cd /Users/tomiaa/Documents/github/vscode-word-dict-practice
code .

# 2. 直接按 F5 键
# VSCode会自动读取 .vscode/launch.json 配置
# 自动打开一个"扩展开发宿主"窗口
# 在新窗口中测试你的插件
```

**⚠️ 常见问题：**

- **如果按 F5 没反应**：确保你在项目根目录打开VSCode（包含 package.json 的目录）
- **如果提示选择环境**：选择 "VS Code Extension Development"
- **如果只看到 Node.js/Chrome**：说明没有在正确的文件夹打开，重新用 `code .` 打开项目根目录

**方法二：使用运行和调试面板**

1. 点击左侧活动栏的"运行和调试"图标（或按 `Cmd+Shift+D`）
2. 在顶部下拉菜单选择 "运行扩展"
3. 点击绿色播放按钮 ▶️

### 2. 调试技巧

- **查看日志**：在调试窗口的"调试控制台"查看 `console.log()` 输出
- **设置断点**：在 `extension.js` 中点击行号左侧设置断点
- **调试Webview**：
  1. 右键点击侧边栏的Webview内容
  2. 选择"检查"或"打开开发者工具"
  3. 可以查看HTML、CSS和JavaScript

### 3. 修改代码后重新加载

- 按 `Cmd+R` (Mac) 或 `Ctrl+R` (Windows/Linux) 重新加载扩展开发宿主窗口
- 或者点击调试工具栏的重启按钮 🔄

---

## 📦 打包插件

### 1. 安装打包工具

```bash
# 全局安装 vsce (VSCode Extension 打包工具)
npm install -g @vscode/vsce

# 或者使用 npx (不需要全局安装)
npx @vscode/vsce --version
```

### 2. 准备打包

确保 `package.json` 中有以下字段：

- `name`: 插件名称（小写，不能有空格）
- `version`: 版本号
- `publisher`: 发布者名称（必需，用于发布）
- `engines.vscode`: 支持的VSCode版本

### 3. 打包成 .vsix 文件

```bash
# 方法1：使用 npx（推荐）
npx @vscode/vsce package

# 方法2：如果已全局安装 vsce
vsce package

# 打包特定版本
npx @vscode/vsce package --version 0.0.2

# 生成的文件：word-dict-practice-0.0.1.vsix
```

### 4. 本地安装 .vsix 文件测试

**方法一：通过VSCode界面**

1. 打开VSCode
2. 点击左侧"扩展"图标
3. 点击 `...` 菜单 → "从 VSIX 安装..."
4. 选择生成的 `.vsix` 文件

**方法二：通过命令行**

```bash
code --install-extension word-dict-practice-0.0.1.vsix
```

---

## 🚀 发布到市场

### 1. 创建 Azure DevOps 账号

VSCode插件市场需要通过 Azure DevOps 进行身份验证。

1. 访问 [Azure DevOps](https://dev.azure.com/)
2. 使用 Microsoft 账号登录或注册

### 2. 创建 Personal Access Token (PAT)

1. 登录 Azure DevOps
2. 点击右上角用户图标 → "Personal access tokens"
3. 点击 "+ New Token"
4. 配置：
   - **Name**: VSCode Extension Publishing
   - **Organization**: All accessible organizations
   - **Expiration**: 设置过期时间（建议90天或自定义）
   - **Scopes**: 选择 "Custom defined"
     - 勾选 **Marketplace** → **Manage**
5. 点击 "Create"
6. **重要**：复制生成的token（只显示一次！）

### 3. 创建发布者账号

```bash
# 创建发布者
npx @vscode/vsce create-publisher your-publisher-name

# 会提示输入：
# - Publisher name (发布者名称)
# - Display name (显示名称)
# - Email (邮箱)
# - Personal Access Token (刚才创建的PAT)
```

或者直接在网站上创建：[Visual Studio Marketplace Publisher Management](https://marketplace.visualstudio.com/manage)

### 4. 登录发布者账号

```bash
npx @vscode/vsce login your-publisher-name
# 输入你的 Personal Access Token
```

### 5. 更新 package.json

确保 `publisher` 字段与你的发布者名称一致：

```json
{
  "publisher": "your-publisher-name"
}
```

### 6. 发布插件

```bash
# 发布插件
npx @vscode/vsce publish

# 发布并升级版本
npx @vscode/vsce publish patch  # 0.0.1 -> 0.0.2
npx @vscode/vsce publish minor  # 0.0.1 -> 0.1.0
npx @vscode/vsce publish major  # 0.0.1 -> 1.0.0

# 发布特定版本
npx @vscode/vsce publish 1.0.0
```

### 7. 更新已发布的插件

```bash
# 修改代码后
npx @vscode/vsce publish patch
```

---

## 📋 发布前检查清单

- [ ] `package.json` 中填写完整信息：
  - [ ] `name` (全小写，无空格)
  - [ ] `displayName` (显示名称)
  - [ ] `description` (描述)
  - [ ] `version` (版本号)
  - [ ] `publisher` (发布者)
  - [ ] `repository` (代码仓库，可选但推荐)
  - [ ] `icon` (图标文件，可选)
  - [ ] `keywords` (关键词，可选)
  - [ ] `license` (许可证，可选)
- [ ] README.md 文件完善（会显示在市场页面）
- [ ] 测试插件功能正常
- [ ] 检查是否有linter错误

---

## 🔧 常用命令总结

```bash
# 开发
code .                          # 打开项目
# 按 F5 运行调试

# 打包
npx @vscode/vsce package        # 打包成 .vsix

# 本地安装测试
code --install-extension word-dict-practice-0.0.1.vsix

# 发布
npx @vscode/vsce login your-publisher-name
npx @vscode/vsce publish        # 发布
npx @vscode/vsce publish patch  # 发布并升级补丁版本

# 卸载
code --uninstall-extension your-publisher-name.word-dict-practice
```

---

## 📚 参考资源

- [VSCode 扩展 API 文档](https://code.visualstudio.com/api)
- [发布扩展](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)
- [扩展市场](https://marketplace.visualstudio.com/)
- [扩展管理后台](https://marketplace.visualstudio.com/manage)

---

## ⚠️ 注意事项

1. **Personal Access Token**：妥善保管，不要提交到代码仓库
2. **插件名称**：发布后不能修改 `name` 字段
3. **版本号**：遵循语义化版本规范（Semantic Versioning）
4. **审核时间**：首次发布可能需要几分钟到几小时的审核时间
5. **README重要性**：用户首先看到README，请写清楚功能和使用方法

