# 创建 tomiaa 发布者完整步骤

## ⚠️ 重要提示

你需要创建**两个东西**：
1. **Personal Access Token** (Azure DevOps) - 密钥
2. **发布者账号** (Visual Studio Marketplace) - 发布者身份

## 📝 步骤 1：创建 Personal Access Token

### 1.1 访问 Azure DevOps Token 页面

```
https://dev.azure.com/1178852449/_usersSettings/tokens
```

### 1.2 创建新 Token

1. 点击 **"+ New Token"** 按钮
2. 填写配置：

```
┌─────────────────────────────────────────────┐
│ Create a new personal access token         │
├─────────────────────────────────────────────┤
│ Name: VSCode Extension Publishing          │  ← 随便起名，方便记忆
│                                             │
│ Organization: All accessible organizations │
│                                             │
│ Expiration (UTC): Custom defined           │
│ [选择日期] 90 days                          │
│                                             │
│ Scopes: Custom defined                     │
│   ☐ Full access                            │
│   ☑ Marketplace                            │  ← 重要！必须勾选
│     ☑ Acquire                              │
│     ☑ Manage                               │  ← 必须勾选这个
│                                             │
│            [Cancel]  [Create]              │
└─────────────────────────────────────────────┘
```

3. 点击 **"Create"**

4. **⚠️ 立即复制生成的 Token**

```
Token 示例：
eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiIsIng1dCI6...（很长一串）
```

**注意**：
- ⚠️ 这个 Token **只显示一次**！
- 💾 立即保存到安全的地方
- 🔑 这是你的密钥，不要分享给别人

## 📝 步骤 2：创建发布者 `tomiaa`

### 2.1 方法一：通过网页创建（推荐）

1. 访问：
```
https://marketplace.visualstudio.com/manage/createpublisher
```

2. 填写信息：

```
┌─────────────────────────────────────────────┐
│ Create a publisher                         │
├─────────────────────────────────────────────┤
│ Publisher ID*: tomiaa                      │  ← 唯一ID，不能改
│                                             │
│ Publisher display name*: Tomiaa            │  ← 显示名称
│                                             │
│ Email*: 你的邮箱                            │
│                                             │
│            [Cancel]  [Create]              │
└─────────────────────────────────────────────┘
```

3. 点击 **"Create"**

### 2.2 方法二：通过命令行创建

```bash
cd /Users/tomiaa/Documents/github/blog/src/vscodeExtension/WordDictPractice

npx @vscode/vsce create-publisher tomiaa
```

会提示输入：

```
Publisher human-friendly name: Tomiaa
E-mail: 你的邮箱@example.com
Personal Access Token: [粘贴步骤1创建的 token]
```

## 📝 步骤 3：使用 Token 登录

```bash
cd /Users/tomiaa/Documents/github/blog/src/vscodeExtension/WordDictPractice

npx @vscode/vsce login tomiaa
```

提示输入 Token 时，粘贴步骤1创建的 Personal Access Token。

## 📝 步骤 4：发布扩展

```bash
# 确保 package.json 中 publisher 是 "tomiaa"
# 然后发布
npx @vscode/vsce publish
```

## ❓ 常见问题

### Q1: Name 和 ID 有什么区别？

**Azure DevOps Token 的 Name**：
- 只是给这个 token 的备注
- 可以随便起，如 "发布插件用的密钥"
- 只是帮你记住这个 token 是干什么的

**Marketplace Publisher ID**：
- 这是发布者的唯一标识
- 不能修改！
- 会出现在扩展的完整ID中：`tomiaa.word-dict-practice`

**Marketplace Display Name**：
- 这是显示给用户看的名字
- 可以修改
- 会显示在扩展市场

### Q2: 为什么要创建两个账号？

不是创建两个账号，而是：
1. **Token**：证明你的身份（密钥）
2. **Publisher**：你的发布者身份（像个人品牌）

类比：
- Token = 你的身份证
- Publisher = 你的公司名

### Q3: 如果我已经有 tomiaa12 了，还能创建 tomiaa 吗？

可以！一个 Microsoft 账号可以创建多个发布者。

你可以同时拥有：
- `tomiaa12`
- `tomiaa`
- `tomiaa-dev`
- 等等...

### Q4: Token 过期了怎么办？

1. 重新访问：https://dev.azure.com/1178852449/_usersSettings/tokens
2. 创建新的 Token（步骤同上）
3. 重新登录：`npx @vscode/vsce login tomiaa`

## ✅ 验证

创建成功后，访问：
```
https://marketplace.visualstudio.com/manage/publishers/tomiaa
```

应该能看到你的发布者页面。

## 🎉 完成

现在你就可以用 `tomiaa` 发布扩展了！

